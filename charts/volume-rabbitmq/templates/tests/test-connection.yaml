apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "volume-rabbitmq.fullname" . }}-test-connection"
  labels:
    {{- include "volume-rabbitmq.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "volume-rabbitmq.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "volume-rabbitmq.fullname" . }}
  labels:
    {{- include "volume-rabbitmq.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "volume-rabbitmq.selectorLabels" . | nindent 4 }}

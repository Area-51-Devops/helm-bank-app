apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "service-service.fullname" . }}-test-connection"
  labels:
    {{- include "service-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "service-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

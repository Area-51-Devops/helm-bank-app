apiVersion: v1
kind: Service
metadata:
  name: {{ include "volume-mysql.fullname" . }}
  labels:
    {{- include "volume-mysql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "volume-mysql.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fraud-service.fullname" . }}-test-connection"
  labels:
    {{- include "fraud-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fraud-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Chart.Name }}-account-hpa-stable
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Chart.Name }}-account-deployment-stable
  minReplicas: {{ .Values.stable.hpa.minReplicas }}
  maxReplicas: {{ .Values.stable.hpa.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.stable.hpa.resource.cpu.UtilizationPercentage }} 
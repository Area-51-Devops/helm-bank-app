apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "volume-redis.fullname" . }}-test-connection"
  labels:
    {{- include "volume-redis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "volume-redis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

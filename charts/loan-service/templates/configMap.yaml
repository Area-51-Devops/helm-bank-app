apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-loan-config
  namespace: {{ .Values.namespace }}
data:
  DB_HOST: {{ .Chart.Name }}-mysql-service
  DB_USER: {{ .Values.env.dbUser }}
  DB_NAME: {{ .Values.env.dbName }}
  PORT: "{{ .Values.port }}"